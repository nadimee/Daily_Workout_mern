{
  "dependencies": {
    "bcrypt": "^5.1.0",
    "date-fns": "^2.29.3",
    "jsonwebtoken": "^9.0.0",
    "validator": "^13.9.0"
  },
  "name": "mern_daily_workout_app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i concurrently && cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "author": "nadime",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}

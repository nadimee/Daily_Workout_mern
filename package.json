{
  "dependencies": {
    "bcrypt": "^5.1.0",
    "date-fns": "^2.29.3",
    "jsonwebtoken": "^9.0.0",
    "validator": "^13.9.0"
  },
  "name": "mern_daily_workout_app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm start",
    "start": "cd backend && npm start"
  },
  "author": "nadime",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
